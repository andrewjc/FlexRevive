# The Visual Studio CMake generator does not emit compile_commands.json, so clangd would
# otherwise fall back to C++14 without NOMINMAX and flag every std::min/std::max in the
# solver as a syntax error (Windows.h defines min/max as macros).
CompileFlags:
  Add:
    - -std=c++20
    - -DNOMINMAX
